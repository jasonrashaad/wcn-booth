# Local build truth and anything about Jason himself stay off GitHub.
# The persona (corpus, extracts, model files) moved to wcn-coach (private) 2026-09-20;
# these patterns stay so nothing of that shape can ever land here again.
CLAUDE.md
persona/
episodes/
.venv/
__pycache__/
.DS_Store
*.log
